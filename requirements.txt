# BL RCA Agent — exact pinned dependencies
# Generated from pip freeze, filtered to project imports only

# API server
fastapi==0.109.0
uvicorn==0.41.0
python-multipart==0.0.20

# HTTP client (LLM Gateway calls)
httpx==0.28.1

# DWH (direct Redshift connection)
psycopg2-binary==2.9.10

# Data loading (CSV parsing)
pandas==2.3.1

# LLM clients
anthropic==0.79.0
parallel-web==0.4.2

# Config / env
python-dotenv==1.2.2

# Observability (optional — falls back to no-op if keys not set)
langfuse==4.0.6

# Testing
pytest==9.0.2
pytest-mock==3.15.1
